# Static site for join-social.com (Cloudflare Workers + Static Assets).
name = "join-social"
compatibility_date = "2026-07-29"
main = "worker.js"

[assets]
directory = "."
binding = "ASSETS"
# Only invoke the worker for dynamic reset links; everything else is static.
run_worker_first = ["/reset-password/*"]
